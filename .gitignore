node_modules/
dist/
.astro/
coverage/
*.log
.DS_Store
*.sqlite
*.sqlite-journal

# `frogcp schema > schema.sql`, generated on demand by the examples README steps.
schema.sql

# Written and removed by examples/cloudflare/scripts/live-verify.sh. Ignored so
# an interrupted run cannot leave them staged.
wrangler.livetest*.jsonc
worker.migratecheck.ts
